[project]
name = "leetcode-env-creator"
version = "0.1.0"
description = "Fetch LeetCode problem data and generate code templates with test skeletons"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "lxml>=6.0.2",
]
